# VSCode Neovim 配置说明

> 本仓库包含了我个人在 VSCode 中使用 Neovim 的配置，包括插件管理、按键映射和自定义设置。

## 功能特色

- 使用 lazy.nvim 进行插件管理
- 常用插件：vim-surround、vscode-neovim-multicursor、hop.nvim 等
- 高效的自定义按键映射
- 兼容 VSCode Neovim 插件

## 快速开始

1. 克隆本仓库：
   ```sh
   git clone https://github.com/MTsocute/nvim ~/.config/nvim
   ```
2. 打开 VSCode，确保已安装 Neovim 插件。
3. 享受更强大的 Neovim 开发体验！

## 快捷键配置

### 编辑器导航

| 快捷键    | 模式       | 说明                 |
| --------- | ---------- | -------------------- |
| `J`     | 普通、可视 | 上一个编辑器标签     |
| `K`     | 普通、可视 | 下一个编辑器标签     |
| `<C-h>` | 普通       | 焦点移至左侧编辑器组 |
| `<C-l>` | 普通       | 焦点移至右侧编辑器组 |
| `<C-j>` | 普通       | 焦点移至下方编辑器组 |
| `<C-k>` | 普通       | 焦点移至上方编辑器组 |

### 文件操作

| 快捷键         | 模式 | 说明       |
| -------------- | ---- | ---------- |
| `<leader>ff` | 普通 | 格式化文档 |

### 编辑器管理

| 快捷键         | 模式 | 说明           |
| -------------- | ---- | -------------- |
| `<leader>cc` | 普通 | 关闭当前编辑器 |
| `<leader>ca` | 普通 | 关闭所有编辑器 |

### 搜索与导航 (类 Telescope)

| 快捷键         | 模式 | 说明                 |
| -------------- | ---- | -------------------- |
| `<leader>fa` | 普通 | 查找文件（快速打开） |
| `<leader>fg` | 普通 | 全局搜索文字         |
| `<leader>fw` | 普通 | 在当前文件中查找     |

### 代码折叠

| 快捷键 | 模式 | 说明         |
| ------ | ---- | ------------ |
| `zM` | 普通 | 折叠所有     |
| `zR` | 普通 | 展开所有     |
| `zc` | 普通 | 折叠         |
| `zC` | 普通 | 递归折叠     |
| `zo` | 普通 | 展开         |
| `zO` | 普通 | 递归展开     |
| `za` | 普通 | 切换折叠状态 |

### 其他

| 快捷键    | 模式 | 说明         |
| --------- | ---- | ------------ |
| `<Esc>` | 普通 | 清除搜索高亮 |

### 插件：Hop.nvim (快速跳转)

| 快捷键                 | 模式 | 说明                   |
| ---------------------- | ---- | ---------------------- |
| `<leader><leader>w`  | 全部 | 跳转到单词             |
| `<leader><leader>l`  | 全部 | 跳转到行               |
| `<leader><leader>co` | 全部 | 跳转到单个字符         |
| `<leader><leader>cd` | 全部 | 跳转到两个字符         |
| `<leader><leader>f`  | 普通 | 使用正则表达式模式跳转 |

### 插件：VSCode 多光标

| 快捷键        | 模式       | 说明                       |
| ------------- | ---------- | -------------------------- |
| `mc`        | 普通、可视 | 创建光标                   |
| `mcc`       | 普通       | 取消/清除所有光标          |
| `mi`        | 普通、可视 | 在左侧启动光标             |
| `mI`        | 普通、可视 | 在最左侧启动光标           |
| `ma`        | 普通、可视 | 在右侧启动光标             |
| `mA`        | 普通、可视 | 在最右侧启动光标           |
| `[mc`       | 普通       | 跳转到上一个光标           |
| `]mc`       | 普通       | 跳转到下一个光标           |
| `mcs`       | 普通       | 使用 flash 创建光标        |
| `mcw`       | 普通       | 使用 flash 创建选择        |
| `<leader>p` | 普通       | 选择当前单词，跳转到上一个 |
| `<leader>n` | 普通       | 选择当前单词，跳转到下一个 |
| `<leader>x` | 普通       | 取消当前光标处的多光标选择 |

### 插件：vim-surround

vim-surround 提供了方便地添加、修改或删除包围字符（引号、括号等）的命令。

常用命令：

- `ys{motion}{char}` - 添加包围
- `cs{old}{new}` - 修改包围
- `ds{char}` - 删除包围
- `S{char}` (可视模式) - 包围选择内容

示例：

- `ysiw"` - 用 " 包围单词
- `cs"'` - 将 " 改为 '
- `ds"` - 删除周围的 "

> **注意**: `<leader>` 键映射为 `空格键`

## 文档

- [English Documentation](./README.md)
